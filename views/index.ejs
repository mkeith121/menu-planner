<!doctype html>
<html>
  <head>
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.5.3/angular.min.js"></script>
  </head>
  <body ng-app="menuApp">
    <div ng-controller="welcomeController">
      <input type= "text" ng-model="name">
      <h2>Hello there, {{ name }}</h2>
      <h3>Today is {{ day | date: 'EEEE, MMMM d, y' }}</h3>
      Helllo world
    </div>
    <div ng-controller="entryController">
    <h2>Menu Entry:</h2>
      <form id="menuEntry" ng>
        <input type="text" id="menuText" placeholder="Whatcha Wanna Eat?" ng-model="entry.input">
        <br>
        <select id="day" ng-model="entry.day" value="Day">
          <option value="" disabled selected>Day</option>
          <option value="Sunday" />Sunday
          <option value="Monday" />Monday
          <option value="Tuesday" />Tuesday
          <option value="Wednesday" />Wednesday
          <option value="Thursday" />Thursday
          <option value="Friday" />Friday
          <option value="Saturday" />Saturday
        </select>
        <select id="meal" ng-model="entry.meal" value="Meal">
          <option value="" disabled selected>Occasion</option>
          <option value="Breakfast" />Breakfast
          <option value="Lunch" />Lunch
          <option value="Dinner" />Dinner
          <option value="Snacks" />Snacks
        </select>
        <input type="submit" value="Add to Menu" ng-click="addEntry(entry)">
        <br>
        <br>

        <pre>entry = {{ entry }}</pre>
        <pre>entries = {{ entries }}</pre>

      </form>
    <div>

<!--       This is the table I actually want -->

      <table border=3 width=50% cellspacing=3 cellpadding=3 ng-repeat="each in entries">
        <tr>
          <td rowspan="4" align=center width="200">{{ each.day }}</td>
          <td align=center width="200">Breakfast</td>
          <td align=center width="200">{{ each.entry.Breakfast }}</td>
        </tr>
        <tr>
          <td align=center width="200">Lunch</td>
          <td align=center width="200">{{ each.entry.Lunch }}</td>
        </tr>
        <tr>
          <td align=center width="200">Dinner</td>
          <td align=center width="200">{{ each.entry.Dinner }}</td>
        </tr>
        <tr>
          <td align=center width="200">Snacks</td>
          <td align=center width="200">{{ each.entry.Snacks }}</td>
        </TR>
      </table>

    </div>

    </div>
    <script src="/menuApp.js"></script>
    <script src="/entryController.js"></script>
    <script src="/welcomeController.js"></script>
    <script src="/entryService.js"></script>
  </body>
</html>